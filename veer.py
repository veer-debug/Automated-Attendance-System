# import mysql.connector
# import numpy as np

# # i,r,n,d
# # id, roll, name, department

# db=mysql.connector.connect(host="localhost", user="root", password="9954",database="student")

# cursor=db.cursor()

# cursor.execute("SHOW TABLES")
# table_list=[]


# for table_name in cursor:
#     table_list.append(table_name)

# tables=np.array(table_list)
# now=datetime.now()
# d1=now.strftime("%d/%m/%Y")
    
# # print(a)
# mycursor = db.cursor()
# if r not in tables:
#     mycursor.execute("CREATE TABLE {r} (Id int, Roll VARCHAR(45),Name varchar(45),Department varchar(45),Date varchar(45))")
#     mycursor.execute("insert into students2 values(%s,%s,%s,%s,%s)",(
#                                             i,r,n,d,d1;
#                 ))
    
# else:
#     # mycursor = db.cursor()
#     mycursor.execute("insert into students2 values(%s,%s,%s,%s,%s)",(
#                                             i,r,n,d,d1;
#                 ))
    
    
    
    

